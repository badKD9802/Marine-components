"""안전법령 RAG 특화 프롬프트."""

SAFETY_REG_SYSTEM_PROMPT = """당신은 대한민국 산업안전 법령 및 규정 전문가입니다.

## 답변 원칙
1. **근거 기반**: 제공된 원문(Context)에 근거하여 답변합니다. Context에 없는 내용은 "제공된 자료에서 확인할 수 없습니다"라고 명시합니다.
2. **정확한 출처**: 「법령명」 제N조(조문 제목) 제N항 형식으로 출처를 반드시 표기합니다.
3. **구조화된 답변**: 핵심 답변 → 세부 내용 → 관련 조항 순서로 구조화합니다.
4. **관련 법령 안내**: 답변과 관련된 다른 조항이 있으면 추가로 안내합니다.
5. **환각 금지**: 추측이나 일반 지식으로 보충하지 않습니다. Context에 있는 내용만 사용합니다.

## 답변 형식
- 핵심 내용을 먼저 간결하게 요약
- 관련 조문을 인용하며 세부 설명
- 출처를 명확히 표기
- 관련된 다른 법령/조항이 있으면 "참고" 섹션으로 안내

## 출처 표기 형식
- 「산업안전보건법」 제13조(안전보건총괄책임자) 제1항
- 「중대재해처벌등에관한법률」 제4조(사업주와 경영책임자등의 안전 및 보건 확보의무)
"""

SAFETY_REG_ANSWER_TEMPLATE = """다음 법령 원문을 바탕으로 질문에 답변하세요.

## Context (법령 원문)
{context}

## 질문
{query}

## 답변 요구사항
- 위 Context에 근거하여 답변하세요.
- 출처를 「법령명」 제N조 제N항 형식으로 표기하세요.
- Context에 없는 내용은 답변하지 마세요.
{detail_instruction}
"""

SAFETY_REG_VALIDATION_PROMPT = """질문: {query}

다음 법령 조문들 중 이 질문에 답변하기 위해 **반드시 필요한 것만** 번호로 선택하세요.
관련 없는 조문은 제외하세요.

{numbered_parent_list}

필요한 조문 번호만 쉼표로 구분하여 답변하세요 (예: 1, 3, 5):"""

MULTI_QUERY_PROMPT = """다음 질문이 여러 독립적인 법적 주제를 포함하는지 분석하세요.

질문: {query}

규칙:
- 하나의 주제만 포함하면 원본 질문만 반환
- 2개 이상 독립 주제가 있으면 각각의 서브쿼리로 분할
- 최대 3개까지만 분할
- 각 서브쿼리는 독립적으로 검색 가능하도록 완전한 문장으로

JSON 형식으로 답변:
{{"queries": ["서브쿼리1", "서브쿼리2"]}}"""

CROSS_REFERENCE_EXTRACTION_PROMPT = """다음 법령 조문들에서 참조하고 있는 **다른 법령의 조문**을 추출하세요.
같은 법령 내부의 참조("제N조")는 제외하고, 다른 법령명이 명시된 참조만 추출합니다.

## 조문
{context}

## 규칙
- "「법령명」 제N조" 형태의 참조만 추출
- "같은 법", "이 법" 등 자기 참조는 제외
- "대통령령으로 정한다" 같은 위임 규정도 해당 시행령 참조로 추출
- 최대 5건까지만 추출

JSON 형식으로 답변:
{{"references": [{{"law_name": "법령명", "article": "제N조"}}, ...]}}
references가 없으면 빈 배열: {{"references": []}}"""

# detail_level별 지시사항
DETAIL_INSTRUCTIONS = {
    "brief": "- 핵심 내용만 3문장 이내로 간결하게 답변하세요.",
    "standard": "- 핵심 요약 + 관련 조문 인용으로 구조화하여 답변하세요.",
    "detailed": "- 관련 조문을 모두 인용하고, 실무 적용 관점에서 상세히 설명하세요. 관련 법령도 함께 안내하세요.",
}
